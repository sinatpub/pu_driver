/// The list itself (`items`, `isLoading`, `hasReachedMax`, `errorMessage`)
/// lives on [PaginatedController], shared with the history screen — same
/// documented exception to `14` §3.3 noted in `history/state.dart`.
class AnnouncementState {}
